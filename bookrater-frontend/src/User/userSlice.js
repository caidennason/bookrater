import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
    name: "users",
    initialState:{
        enitities: []
    } ,
    reducers: {

    },
    extraReducers: {

    }
})

export default userSlice.reducer